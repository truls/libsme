proc plusone(in inbus, const val)
    exposed bus plusout {
       val: i32;
    };
{
    plusout.val = inbus.val + 1;
}

network plusone_net() {
    exposed bus idout {
        valid: bool;
        val: i32;
    };

    instance plusone_inst of
        plusone(idout);
}
